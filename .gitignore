# OS artifacts
.DS_Store
Thumbs.db

# Editor artifacts
.vscode/
.idea/

# Astro build output and dependencies
dist/
node_modules/
.astro/

# Wrangler local state
.wrangler/

# Cloudflare Pages build cache
.cache/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Claude Code agent memory (machine-generated, local-only)
.claude/agent-memory/

# Environment / secrets
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example
*.secret
*.pem
*.key

# Codex プラグインのローカル生成物(絶対パスを含む個人ローカル層)
.codex/

# Codex CLI / agent-assets symlink (~/.agents) / pi のローカル生成物・個人状態。
# いずれも絶対パスや個人環境固有の値を含みうる個人ローカル層で、
# 共有すると他の環境で壊れる (keroway/agent-assets#186)
.agents/
.pi/
